<%= form_for(@magister) do |f| %>
  <% if @magister.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magister.errors.count, "error") %> prohibited this magister from being saved:</h2>

      <ul>
      <% @magister.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
